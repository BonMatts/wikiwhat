source 'https://rubygems.org'

# Specify your gem's dependencies in wikiwhat.gemspec

gem 'rspec'
gem 'json'

# For recording HTTP requests so we can re-use them in our tests.
gem 'vcr'

# Required for VCR
gem 'webmock', '~> 1.8.0'

# For making our API calls
gem 'rest-client'
